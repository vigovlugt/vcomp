// import { sortByFrequency } from "../utils/Frequency";

// test("Frequency sorting works", () => {
//   const input = "abcddefff,xx";
//   const sorted = sortByFrequency(input);
//   expect(sorted.toString()).toBe(
//     ["f", "d", "x", "a", "b", "c", "e", ","].toString()
//   );
// });
